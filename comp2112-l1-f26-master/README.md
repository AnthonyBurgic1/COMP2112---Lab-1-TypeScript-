## Lesson 1

### NPM Commands to run in Terminal
`npm i typescript` - install TypeScript in this project folder

`npx tsc --init` - install and run TypeScript compiler to transpile TS to JS

`tsc` - transpile TS to JS